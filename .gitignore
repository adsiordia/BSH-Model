# per-residue embeddings: 170-230 MB each, over GitHub's limit.
# Only the pooling sweep needs them; see README for how to regenerate.
*_per_residue.h5

__pycache__/
*.pyc
logs/
slurm-*.out
site/.serve.pid
site/.serve.log
.DS_Store
